import { Person } from "./person";

export const convertToStringArray = (value: String): Array<String> => {
  return [value];
};

export const convertToNumberArray = (value: Number): Array<Number> => {
  return [value];
};

export const convertToPersonArray = (value: Person): Array<Person> => {
  return [value];
};

export const convertToValueArray = (value: any): Array<any> => {
  return [value];
};
